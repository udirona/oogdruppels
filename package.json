{
  "name": "oogdruppels",
  "version": "1.0.0",
  "description": "oggdruppels db",
  "main": "index.js",
  "scripts": {
    "build": "rimraf build && tsc",
    "start": "nodemon --exec ts-node app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^14.11.2"
  }
}
